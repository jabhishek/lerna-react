import { ResponsiveValue } from "styled-system";
export declare const getStyles: (props: {
    width: ResponsiveValue<string | number>;
    fullWidth: boolean;
}) => {
    width: any;
    p: string[];
    borderRadius: string;
    color: string;
};
